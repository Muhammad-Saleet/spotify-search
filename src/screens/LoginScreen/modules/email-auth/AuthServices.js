export default {
  async login (credentials) {
    return { error: 'not implemented' }
  },
  async register (user) {
    // register a new user upon sign up
  },
  async resetPassword (email) {
    // reset a user's password
  }
}
